"use client";
import Link from"next/link";import{useParams}from"next/navigation";import{AlertTriangle}from"lucide-react";import{isLocale,type Locale}from"@/lib/i18n/config";import{translate}from"@/lib/i18n/dictionaries";import{Button,buttonStyles}from"@/components/ui/button";
export default function ErrorPage({reset}:{error:Error&{digest?:string};reset:()=>void}){const params=useParams<{locale?:string}>();const rawLocale=params.locale??"";const locale:Locale=isLocale(rawLocale)?rawLocale:"en";return <div className="container-shell py-24"><section className="mx-auto max-w-xl rounded-3xl border border-line bg-surface p-10 text-center shadow-soft"><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-red-700 text-white"><AlertTriangle className="size-7"/></span><h1 className="mt-6 font-display text-4xl font-semibold">{translate(locale,"error.title")}</h1><p className="mt-3 text-muted">{translate(locale,"error.text")}</p><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Button onClick={reset}>{translate(locale,"common.retry")}</Button><Link href={`/${locale}`} className={buttonStyles({variant:"secondary"})}>{translate(locale,"nav.home")}</Link></div></section></div>}
