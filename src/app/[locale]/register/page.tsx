import { notFound } from "next/navigation";import { isLocale } from "@/lib/i18n/config";import { getStorefrontLocations } from "@/lib/catalog/locations";import { AuthForm } from "@/components/storefront/auth-form";
export default async function RegisterPage({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{next?:string}>}){const[{locale:raw},query]=await Promise.all([params,searchParams]);if(!isLocale(raw))notFound();const locations=await getStorefrontLocations(raw);return <div className="container-shell max-w-5xl py-12 md:py-16"><AuthForm locale={raw} mode="register" nextPath={query.next} locations={locations}/></div>}
