"use client";
import Link from"next/link";import{useParams}from"next/navigation";import{PackageX}from"lucide-react";import{isLocale,type Locale}from"@/lib/i18n/config";import{translate}from"@/lib/i18n/dictionaries";import{buttonStyles}from"@/components/ui/button";
export default function NotFound(){const params=useParams<{locale?:string}>();const rawLocale=params.locale??"";const locale:Locale=isLocale(rawLocale)?rawLocale:"en";return <div className="container-shell py-24"><section className="mx-auto max-w-xl rounded-3xl border border-line bg-surface p-10 text-center"><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-brand-strong text-white"><PackageX className="size-7"/></span><p className="mt-6 text-xs font-black uppercase tracking-[.25em] text-accent">404</p><h1 className="mt-3 font-display text-4xl font-semibold">{translate(locale,"notFound.title")}</h1><p className="mt-3 text-muted">{translate(locale,"notFound.text")}</p><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Link href={`/${locale}/products`} className={buttonStyles()}>{translate(locale,"nav.products")}</Link><Link href={`/${locale}`} className={buttonStyles({variant:"secondary"})}>{translate(locale,"nav.home")}</Link></div></section></div>}
